describe('Pacientes',()=>{it('busca, crea y elimina un paciente',()=>{cy.visit('/pacientes');cy.get('[data-cy="patient-search"]').type('Ana');cy.get('[data-cy="patient-row"]').should('have.length',1);cy.get('[data-cy="patient-search"]').clear();cy.get('[data-cy="new-patient"]').click();cy.get('[data-cy="patient-name"]').type('Lucía');cy.get('input[formcontrolname="apellidos"]').type('Prueba');cy.get('input[formcontrolname="usuario"]').type('lucia.p');cy.get('input[formcontrolname="clave"]').type('1234');cy.get('[data-cy="patient-nss"]').type('NSS999');cy.get('input[formcontrolname="numTarjeta"]').type('TS999');cy.get('input[formcontrolname="telefono"]').type('600999999');cy.get('input[formcontrolname="direccion"]').type('Calle Test');cy.get('[data-cy="save-patient"]').click();cy.contains('Lucía Prueba');cy.get('[data-cy="toast"]').should('contain','Paciente guardado');cy.contains('tr','Lucía Prueba').within(()=>cy.contains('Eliminar').click());cy.get('[data-cy="confirm-delete"]').click();cy.contains('Lucía Prueba').should('not.exist')})});
