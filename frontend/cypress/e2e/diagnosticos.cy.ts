describe('Diagnósticos',()=>{it('crea y elimina un diagnóstico',()=>{cy.visit('/diagnosticos');cy.get('[data-cy="new-diagnosis"]').click();cy.get('[data-cy="diagnosis-name"]').type('Alergia');cy.get('[data-cy="diagnosis-value"]').type('Evitar alérgeno y revisar');cy.get('[data-cy="save-diagnosis"]').click();cy.contains('Alergia');cy.contains('tr','Alergia').within(()=>cy.contains('Eliminar').click());cy.get('[data-cy="confirm-delete"]').click();cy.contains('Alergia').should('not.exist')})});
